# Trabajo Práctico - CRUD Categorías + CRUD Productos + CRUD Ingredientes

## OBJETIVO GENERAL

Desarrollar una aplicación Full Stack conectando un frontend React + TypeScript con un backend FastAPI. El estudiante implementará el CRUD completo de Categorías, Productos e ingredientes desde la interfaz de usuario, integrando el backend mediante fetch nativo. Los CRUDs de Pedido y DetallePedido serán construidos en el backend y demostrados mediante un archivo REST Client.

# MARCO TEÓRICO

| Concepto       | Aplicación en el proyecto                                                                                                                                         |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| React          | Biblioteca para construir interfaces de usuario mediante componentes reutilizables.                                                                               |
| TypeScript     | Superset tipado de JavaScript para detectar errores en compilación y mejorar la legibilidad.                                                                      |
| Vite           | Herramienta de build moderna que inicializa proyectos React con TypeScript, reemplazando Create React App.                                                        |
| FastAPI        | Framework Python de alto rendimiento para construir APIs REST. Permite definir endpoints, validar datos con Pydantic y exponer documentación automática en /docs. |
| Pydantic       | Librería de validación de datos usada por FastAPI para definir modelos (schemas) con tipado estricto.                                                             |
| fetch nativo   | API del navegador para realizar peticiones HTTP. Se usa con async/await para consumir la API REST desde el frontend.                                              |
| tanStack form  | Librería para formularios fuertemente tipados y escalables                                                                                                        |
| tanStack table | Librería para el manejo de tablas                                                                                                                                 |
| Interface      | Construcción de TypeScript que define la forma de un objeto (ej: Categoría, Producto).                                                                            |
| Tailwind CSS   | Framework de CSS utilitario. Estilos aplicados directamente en el JSX mediante clases predefinidas.                                                               |
| CORS           | Mecanismo que permite al frontend (localhost:5173) comunicarse con el backend (localhost:8000) en dominios distintos.                                             |

# CASO PRÁCTICO

## Parte A — Backend con Fast Api

Tomando como base el trabajo práctico de la unidad 3:

a) Crear y activar un entorno virtual Python, crea archivo de dependencias e instalar las dependencias:

```python
python -m venv .venv
source .venv/bin/activate  # macOS/Linux
./.venv/Scripts/activate  # Windows
pip install -r requirements.txt
```

b) Definir los models y schemas del módulo Ventas, Pagos &amp; Trazabilidad.
(SVG adjunto a este pdf)

c) Crear un CRUD siguiendo la arquitectura
Models-Schemas-Repository-UoW-Service-Router

## Notas:

Recuerda agregar CORS para poder consumir tu API desde su frontend.

Estructura de ejemplo:

```txt
app/
    core/
        config.py
        database.py
        repository.py
        unit_of_work.py
    modules/
        health/
            router.py
        heroes/
            models.py
            repository.py
            router.py
            schemas.py
            service.py
            unit_of_work.py
    teams/
        models.py
        repository.py
        router.py
        schemas.py
        service.py
        unit_of_work.py
    .env
    main.py
    README.md
    requirements.txt
    seed.py
```

d) La solución deberá de implementar todo lo aprendido en unidades anteriores.

e) Ejecutar el BackEnd:

```bash
python -m fastapi dev main.py
```

# Parte B — Frontend con React + TypeScript (MEJORADO)

f) Páginas (Routing)

La aplicación debe estar organizada en páginas separadas.

### Crear en src/pages/:

- CategoriesPage.tsx
- ingredientsPage.tsx
- ProductsPage.tsx

### CategoriesPage

Debe contener:

- Toda la lógica de categorías:
  - useQuery
  - useMutation
  - manejo del modal
  - tanstack form
  - tanstack table
- Renderiza:
  - CategoriaList
  - CategoriaModal
  - botón “Nueva categoría”

### ProductsPage

- Estructura similar a CategoriasPage
- Preparada para trabajar con productos
- Puede reutilizar lógica (recomendado)
- Debe tener:
  - listado
  - useQuery
  - useMutation
  - manejo del modal
  - tanstack form
  - tanstack table

### IngredientsPage

- Estructura similar a CategoriasPage
- Preparada para trabajar con ingredientes
- Puede reutilizar lógica (recomendado)
- Debe tener:
  - listado
  - useQuery
  - useMutation
  - manejo del modal
  - tanstack form
  - tanstack table

# g) Navegación

Implementar rutas usando React Router.

Instalar:

pnpm add react-router-dom

# Configuración básica

Definir rutas:

- /categories → CategoriasPage
- /products → ProductsPage
- /ingredients → IngredientesPage

# Navbar

Actualizar Navbar.tsx para incluir navegación:

- Link a Categorías
- Link a Productos
- Link a Productos

# h) App.tsx (cambio clave)

Ahora App ya no tiene lógica de negocio.

Solo debe encargarse de:

- Definir rutas
- Renderizar el Navbar
- Mostrar las páginas

# Comportamiento esperado (actualizado)

- La app inicia en /categories
- Se puede navegar a /products
- Se puede navegar a /ingredients
- Cada página maneja su propia lógica
- No hay lógica mezclada en App (orden profesional)

# CONCLUSIONES ESPERADAS

Al finalizar el trabajo práctico, el estudiante debe demostrar:

- Integración Full Stack: El frontend React consume la API FastAPI mediante fetch nativo. El CRUD de Categorías funciona de extremo a extremo.
- Modal con useState: El formulario de alta y edición está dentro de un modal controlado con estado booleano.
- Hooks aplicados correctamente: useState y useEffect utilizados en App.tsx. Los demás componentes son funcionales puros sin hooks.
- Tipado TypeScript: Interfaces Categoría y Producto definidas. Props tipados en todos los componentes .tsx.
- Backend REST documentado: Endpoints de Pedido y DetallePedido funcionando y demostrados en el archivo .http.
- CORS configurado: El backend permite peticiones desde el origen del frontend.

PROGRAMACIÓN IV

TECNICATURA UNIVERSITARIA
EN PROGRAMACIÓN
A DISTANCIA

UTN
TECNICATURA UNIVERSITARIA
EN PROGRAMACIÓN
A DISTANCIA

# RÚBRICA DE EVALUACIÓN

| Criterio                              | Descripción                                                                                                 | Puntos |
| ------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ------ |
| CRUD Categories – Frontend            | Listar, crear, editar y eliminar categorías desde React usando useQuery, tanstackForm, tanstackTable.       | 10     |
| CRUD Ingredients – Frontend           | Listar, crear, editar y eliminar categorías desde React usando useQuery, tanstackForm, tanstackTable        | 10     |
| CRUD Products– Frontend               | Listar, crear, editar y eliminar categorías desde React usando useQuery, tanstackForm, tanstackTable        | 10     |
| Modal funcional                       | Modal que se abre para crear y editar categorías, controlado con useState. Se cierra al guardar o cancelar. | 15     |
| CRUD Pedidos– Backend                 | Endpoints FastAPI GET /pedido, POST, PUT/{id}, DELETE/{id} con Pydantic y persistencia en lista/DB.         | 20     |
| CRUD Pedidos y relación – REST Client | Endpoints de Pedido y DetallePedido demostrados con REST Client (.http). No requiere UI.                    | 15     |
| Tipado TypeScript                     | Interfaces Categoria y Producto tipadas. Props tipados en todos los componentes.                            | 10     |
| Estructura y buenas prácticas         | Carpetas components/ y types/. Tailwind CSS aplicado. Sin errores de TS en consola.                         | 10     |

## RECURSOS ADICIONALES

### Documentación oficial:

- React: https://react.dev/
- TypeScript: https://www.typescriptlang.org/docs/
- Tailwind CSS: https://tailwindcss.com/docs/
